/** @format */

import {AppRegistry} from 'react-native';
import Home from '../Cronometro/src/screen/Home';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Home);
